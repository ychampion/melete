/** Compatibility import for the additive shared learning vocabulary. */
export {
  episodeId,
  type Intervention,
  intervention,
  interventionRequest,
  type JobLearningScope,
  jobLearningScope,
  type ProcedureScope,
  type ProcedureState,
  procedureScope,
  procedureState,
  type VersionEvidence,
} from '@melete/contracts';
