/**
 * @melete/skills is the procedure knowledge that ships with the release: six
 * short Markdown files, the identity file, and the loader that decides which of
 * them one attempt sees. Selection is a string match, never a model call.
 */
export * from './loader.ts';
export * from './tokens.ts';
