# W14 capability audit and proof

## Assumptions

- SHA `9484023cabd32b786cb4d336dec818f441cd0cc1`: the W14 brief's DONE line selects `integration` as the PR base; its later reference to `main` is inconsistent, so this lane targets `integration` and never pushes to `main`.
- Command `git -C C:/Users/gamin/melete-oss-w14 status --short --branch`: work is confined to the existing `lane/w14-capabilities` worktree; no stash or other worktree is used.
- Command `bun install`: the existing `embedded-postgres` devDependency and fixture are retained; tests use the fake provider and at most two workers.
- Log `W14 contract freeze`: the brief requires a proposal and a stop of the affected slice when a contract change is necessary; additive changes are still contract changes and are not silently introduced elsewhere.

## Start — 2026-09-11 19:41 UTC

- SHA `9484023cabd32b786cb4d336dec818f441cd0cc1`: starting integration revision, clean working tree on `lane/w14-capabilities`.
- Command `bun install`: exit 0; `372 packages installed [30.31s]`.
- Command `rg -n 'hook_event|hook_error|principal|shared|context_invalidated' packages/contracts/src`: runtime hook events and shared principal contracts are absent; `context_invalidated` already exists in the responsibility extension.
- Log `packages/contracts/src/runtime.ts:168-218`: the frozen runtime event union has six variants and no hook event or hook error.
- Log `packages/contracts/src/entities.ts:27-35`: the frozen space kinds contain only `personal` and space audiences only `owner`.
- Log `apps/melete/src/db/schema.ts:26-37`: the current owner table has `owner_singleton_idx`; creating a second authenticated principal requires the scoped contract proposal, not an unguarded second row.
- Command `git -C C:/Users/gamin/melete-oss-w14 clone --depth 1 --branch v2026.9.7 https://github.com/NousResearch/hermes-agent.git .hermes-src`: checkout in progress; reported tag commit `2237be355906fbe6065ce1815711eee52b2d646e`.

## Audit and slice boundaries — 2026-09-11 20:05 UTC

- Log `owner steering`: continue at xhigh, solo and sequentially; no subagents were spawned. `REPORT.md`, branch status and the last three commits were reread before continuing.
- Command `git -C C:/Users/gamin/melete-oss-w14 --git-dir=C:/Users/gamin/melete-oss-w14/.hermes-src/.git --work-tree=C:/Users/gamin/melete-oss-w14/.hermes-src rev-parse HEAD`: `2237be355906fbe6065ce1815711eee52b2d646e`; clone completed without changing the pin.
- Command `python packages/runtime-hermes/scripts/audit-pin.py .hermes-src`: `VALID_HOOKS: 37 (hermes_cli/plugins.py:107)`; literal dispatch sites inventoried in `.agents/notes/0021-hermes-capability-audit.md` within the 60-minute audit cap.
- Log `compaction gap`: `agent/conversation_compression.py:3091` uses `event_callback('session:compress', ...)`; `gateway/platforms/api_server.py:2134-2151` does not wire this callback. No registered plugin compaction hook exists.
- Log `skill wiring gap`: `apps/melete/src/jobs/bundle.ts:291-293` emits empty tools, skills and knowledge; selection helpers passing tests do not prove automatic selection in a running job.
- Command `bun run packages/runtime-hermes/scripts/audit-contracts.ts`: exit 0, with a valid `turn_started` control; `runtime_hook_event=false`, `runtime_hook_error=false`, `persisted_hook_event=false`, `responsibility_hook_event=false`, `shared_space_kind=false`, `space_qualified_memory_audience=false`, `skill_audience_round_trip=false`.
- Log `slice 2 STOP`: `.agents/notes/proposed/2026-09-12-w14-durable-hook-contract.md` specifies the additive event and observer bridge requirements; no frozen contract or runtime implementation was changed.
- Log `slice 3 STOP`: `.agents/notes/proposed/2026-09-12-w14-principal-membership-contract.md` specifies principal identity, compatible setup, membership, audience, generation and revocation requirements; no partial multi-principal authority was shipped.
- Command `bun run test:plugin`: exit 0, `20 passed in 10.57s`.
- Command `bun run lint`: exit 0, `Checked 307 files in 1446ms. No fixes applied.`; this preceded the final proposal/diagnostic edits and will be checked again.
- Command `bun run typecheck`: exit 0, `tsc -b && tsc -p apps/web/tsconfig.json --noEmit`; this preceded the final diagnostic control edit.
- Command `bun run compose:check`: exit 0, `compose:check passed (12 checks)`; no Docker runtime isolation claim is made.
- Command `bun test --max-concurrency=2`: baseline run exceeded the three-minute budget and did not finish; it stopped making progress after entering `apps/melete/test/integration/gateway.test.ts`. The captured log is `%TEMP%/melete-w14-baseline-tests.log`.
- Test `two writes to one space at the same time > all of them land, each in its own commit, each attributed to its own caller`: baseline timeout after 5000ms (`5422.00ms` reported), followed by cleanup-related `ENOENT` for `index.md` and `fatal: not a git repository` in its still-running work.
- Test `two writes to one space at the same time > a record carries the trailer of the write that made it, not of its neighbour`: baseline timeout after 5000ms (`5984.00ms` reported).
- Test `persisted event streams > duplicate event delivery emits one stored row and no duplicate frame`: baseline `beforeEach/afterEach hook timed out` (`5016.00ms` reported).
- Command `taskkill.exe /PID 29716 /T /F`: stopped only the W14 test process after verifying its exact command and creation timestamp `2026-09-12T01:15:21.469069+05:30`, together with its disposable Postgres descendants; other lanes' test processes were preserved. Baseline results remain partial, not green.
- Command `git -C C:/Users/gamin/melete-oss-w14 fetch origin integration`: exit 0; the refreshed integration head is still `9484023cabd32b786cb4d336dec818f441cd0cc1`.
- Command `gh pr list --repo ychampion/melete --state all --limit 60 --json number,title,headRefName,baseRefName,state,mergedAt`: W10c/W11/W12 are not integrated; the only matching execution PR shown was open W10a PR #11. Slices 4 and 5 remain gated.

## Focused verification — 2026-09-11 20:11 UTC

- Command `bun test packages/knowledge/src/space.test.ts --test-name-pattern 'two writes to one space' --max-concurrency=2`: exit 0; `3 pass`, `0 fail`, `22 expect() calls`, `[10.49s]`; the two baseline space-write timeouts did not recur in isolation. No implementation fix was made.
- Command `bun test apps/melete/test/integration/events.test.ts --test-name-pattern 'duplicate event delivery emits one stored row and no duplicate frame' --max-concurrency=2`: exit 1; the named test assertion passed in `1235.00ms`, but teardown reported `(fail) persisted event streams > (unnamed) [5015.00ms]` and `a beforeEach/afterEach hook timed out for this test`; final `1 pass`, `1 fail`, `[28.41s]`. This existing fixture failure remains unresolved; it is not a green deduplication suite.
- Command `bun test packages/runtime-hermes/src packages/skills/src --max-concurrency=2`: exit 0; `67 pass`, `0 fail`, `165 expect() calls`, `[4.10s]`. This is the executed evidence for the narrower adapter and selector rows, not the requested real-Hermes capability proof.
- Command `python packages/runtime-hermes/scripts/audit-pin.py .hermes-src`: standalone rerun exit 0; all 37 registered names have literal dispatch references; final log `Literal dispatch sites do not establish HTTP reachability or durable delivery.`
- Log `docs/CAPABILITIES.md`: all nine requested capabilities are marked `missing` with concrete wiring/contract/dependency reasons; existing narrower behaviors carry exact executed test names and their limits.

## Final checks and stop decision — 2026-09-11 20:14 UTC

- Command `bun run lint`: final exit 0; `Checked 308 files in 955ms. No fixes applied.`
- Command `bun run typecheck`: final exit 0 for `tsc -b && tsc -p apps/web/tsconfig.json --noEmit`.
- Command `bun run packages/runtime-hermes/scripts/audit-contracts.ts`: final exit 0; all seven missing-contract probes remain false after the valid control parses. These results establish the contract stop, not implemented capabilities.
- Command `git -C C:/Users/gamin/melete-oss-w14 diff --check`: exit 0, no whitespace errors.
- Command `git -C C:/Users/gamin/melete-oss-w14 diff --name-only -- packages/contracts`: empty output; frozen contracts are untouched.
- Command `git -C C:/Users/gamin/melete-oss-w14 --git-dir=C:/Users/gamin/melete-oss-w14/.hermes-src/.git --work-tree=C:/Users/gamin/melete-oss-w14/.hermes-src status --short`: empty output; the audited upstream checkout is unmodified.
- Command `git -C C:/Users/gamin/melete-oss-w14 fetch origin integration`: final exit 0; `git -C C:/Users/gamin/melete-oss-w14 log -5 --format='%H %s' origin/integration` still begins at `9484023cabd32b786cb4d336dec818f441cd0cc1`.
- Command `gh pr list --repo ychampion/melete --state all --limit 60 --json number,title,headRefName,baseRefName,state,mergedAt`: the final dependency check still shows only open W10a PR #11 among W9/W10/W11/W12 matches; W10c/W11/W12 are unavailable on integration.
- Log `slice 1`: pinned audit, reproducible source/contract diagnostics and the capability matrix are complete. `README.md` accurately calls shared-space/revocation primitives unimplemented and the invitation UI outside v0.1.
- Log `slices 2 and 3`: stopped at the explicit frozen-contract rule with the two proposals under `.agents/notes/proposed/`; no runtime, database, service, fixture or contract implementation was changed.
- Log `slices 4 and 5`: stopped at the dependency gate; the single real-local-Hermes capability-chain test does not exist or pass in this lane. The W3 real-server script was not run and no provider credentials were used.
- Log `DONE not met`: the full suite is not green and the requested capability proof remains missing. The authorized stop outcome is a draft PR to `integration`, with this report and the final matrix; no merge is requested or performed.

## Delivery and final stop — 2026-09-11 20:14 UTC

- SHA `65a5d8a4e8c1b658765163b93bd67fb48ea2cabf`: committed the pinned audit, two diagnostics, two frozen-contract proposals, capability matrix, README correction and report. `git -C C:/Users/gamin/melete-oss-w14 log -1 --format='%H%n%an <%ae>%n%cn <%ce>%n%s'` confirms both author and committer are `ychampion <68075205+ychampion@users.noreply.github.com>`.
- Command `git -C C:/Users/gamin/melete-oss-w14 push -u origin lane/w14-capabilities`: exit 0; the lane branch now tracks `origin/lane/w14-capabilities`. No force push or push to main occurred.
- Command `gh pr create --repo ychampion/melete --base integration --head lane/w14-capabilities --draft --title 'Audit Hermes capabilities and document the blocked proof' --body-file <temp-body>`: exit 0; created https://github.com/ychampion/melete/pull/13.
- Command `gh pr view 13 --repo ychampion/melete --json number,url,baseRefName,headRefName,headRefOid,isDraft,state`: verified `state=OPEN`, `isDraft=true`, `baseRefName=integration`, `headRefName=lane/w14-capabilities`, and audit head `65a5d8a4e8c1b658765163b93bd67fb48ea2cabf` before this append-only report commit.
- Log `final stop`: resume slices 2/3 only after the frozen-contract additions are available; integrate W10c/W11/W12 before slices 4/5; resolve the recorded fixture timeout and run the full suite plus the single real-Hermes capability-chain proof. No passing end-to-end claim or merge is made.

PROOF PENDING: waiting for W10c/W11 to merge

## Resumed authorization — 2026-09-11 20:19 UTC

- Log `orchestrator steering`: additive contract changes are now authorized, including `hook_event`, `hook_error`, principals, memberships, shared spaces and the skill-selector bundle wiring. This supersedes the earlier additive-contract stop; breaking changes still require a proposal and stop.
- Command `until mkdir C:/Users/gamin/.melete-test.lock 2>/dev/null; do sleep 15; done`: every subsequent full `bun test` run will acquire the shared directory lock and release it with `rmdir` on success or failure. Focused runs remain unlocked.
- Log `timeout qualification`: the earlier full-suite and focused teardown timings were obtained without the new full-suite lock. They are historical observations, not a reproduced serialized-suite timeout; only a timeout reproduced under the lock will be reported as a current suite failure.
- SHA `2d3ca94eb5c07d8ddb662d22ce35d203e61b4972`: resumed from the clean pushed W14 branch, solo at xhigh, with draft PR #13 already open to integration.

## Slice 2 implementation checks — 2026-09-11 20:46 UTC

- Command `bun run test:plugin`: `24 passed in 10.35s`, including registered hook ordering, erased payload values, observer exception isolation and copied thread-context isolation.
- Command `bun test apps/melete/test/integration/hooks.test.ts packages/runtime-hermes/src --max-concurrency=2`: exit 0; `47 pass`, `0 fail`, `124 expect() calls`, `[32.37s]`. The database test persists adapter observations, redelivers each event and replays from a stored cursor.
- Command `python packages/runtime-hermes/patches/observer_bridge.py .hermes-src`: applied the three hash-checked observer seams and shared capture module. The source patch adds actual committed compaction observation and per-run HTTP queue binding; it does not change tool decisions.
- Command `uv venv .hermes-venv --python 3.12`, followed by `uv pip install --python .hermes-venv/Scripts/python.exe -e ./.hermes-src` and the pin's `aiohttp==3.14.3`: prepared the local real-server environment. Provider secrets are removed from its child environment; title generation and background review are disabled.
- Test `real Hermes persists lifecycle hooks and a throwing observer without stopping the tool run`: two attempts exposed an empty test catalog. Logs showed `melete: the broker served no tools`; the fixture had requested nonexistent `test.read`. A speculative startup-preload change was removed; the final fixture uses the existing `files.read` connector and checks its catalog before launching Hermes. This is the second and final allowed fix cycle for this check.

- Command `MELETE_HERMES_E2E=1 bun test apps/melete/test/integration/hooks-real.test.ts --max-concurrency=2`: final permitted rerun exit 1, `[61.76s]`; 16 assertions reached, including real session/turn/pre-tool/post-tool/end hooks, persisted `hook_error`, duplicate redelivery and redaction. The final action count was `0`, expected `1`.
- Log `%TEMP%/melete-e2e-home-GREKKk/logs/agent.log:89`: `Tool files.read handler returned unsupported result type: dict`; the existing forwarder returns dictionaries while the pinned registry requires its supported tool-result representation. No successful broker effect or green real-server test is claimed. Two fix cycles are exhausted; this remaining failure is recorded while W14 proceeds to slice 3.

## Slice 3 authority and bundle checks — 2026-09-11 21:15 UTC

- Command `bun run db:generate`: generated `0015_petite_demogoblin.sql` and its snapshot; the reviewed migration adds principals, memberships and optional bindings, then backfills the installation owner, spaces, jobs, sessions and submission ownership. Old attempts retain null bindings so an already issued personal-space capability remains compatible.
- Log `principal authority`: authenticated request identity now scopes job lists, detail reads, event pages, reset snapshots, knowledge/skill headers, approvals and related resources. New jobs, attempts and capability tokens carry the principal and membership generation; shared admission rejects missing or mismatched bindings.
- Log `membership revocation`: the service transaction advances space policy and membership generation, invalidates memory contexts/prepared outputs, persists `context_invalidated`, fences active attempts and cancels the revoked member's queued/waiting work. Regrant advances membership generation again; old capabilities remain stale.
- Test `single-owner authentication against Postgres`: all ten existing authentication cases passed in the initial focused run, including racing setup, password verification, persistent sessions, cookie flags and origin rejection. The combined command did not finish green because the new test's asynchronous rejection matcher required diagnosis.
- Log `principal test diagnosis`: PostgreSQL reported `idle in transaction`, `ClientRead`, query `begin`, and no blocking PIDs while the asynchronous rejection matcher waited. Explicitly awaiting each operation and asserting the caught error code restored progress; the first implementation fix cycle passed the whole scenario in `2109.00ms` with `61 expect() calls`.
- Command `bun test apps/melete/test/integration/principals.test.ts packages/contracts/src/principals.test.ts --max-concurrency=2`: exit 0; `2 pass`, `0 fail`, `73 expect() calls`, `[13.78s]`. The extended scenario checks three selected skills, private-space/API isolation, broker and gateway refusal, stale queued work, regrant, private memory-source handles and invalidated delivered context.
- Command `bun run typecheck`: exit 0 after principal/API/bundle authority wiring; subsequent memory and migration-compatibility edits will receive final checks.
- Command `bun run openapi` and command `bun run client:generate`: both exit 0; the new principal/shared-space/membership paths and optional fields are generated into the published schemas and client types.
- Log `proof boundary`: the lifecycle real-server action assertion remains unverified; the new principal scenario uses the real service, broker, gateway budget, pg-boss and PostgreSQL with the scripted adapter. It does not establish the gated MCP/learning chain.

## Final review and serialized-suite queue — 2026-09-11 21:40 UTC

- Command `bun test apps/melete/test/integration/principals.test.ts --max-concurrency=2`: migration and membership scenarios passed, `2 pass`, `0 fail`, `75 expect() calls`, `[16.00s]`; later private-timeline and receipt-history guards await the final suite.
- Command `bun run typecheck`: exit 0 after the private-timeline and receipt-history changes. Command `bun run lint`: final review exit 0, `Checked 318 files in 1324ms. No fixes applied.` Command `git -C C:/Users/gamin/melete-oss-w14 diff --check`: exit 0.
- Command `bun run compose:check`: exit 0, all 12 configuration assertions pass; this is not a Docker network test. Command `python packages/runtime-hermes/scripts/audit-pin.py .hermes-src`: exit 0, the patched source exposes 38 hook names including `on_compaction`.
- Log `full-suite queue`: the guarded `bun test --max-concurrency=2` command is still waiting for `C:/Users/gamin/.melete-test.lock`. Live process inspection shows another lane's Bun test process; W14 has neither removed its lock nor interrupted it. No serialized-suite timeout is claimed before W14 acquires the lock and runs.
- SHA `9484023cabd32b786cb4d336dec818f441cd0cc1`: a fresh `git -C C:/Users/gamin/melete-oss-w14 fetch origin integration` leaves integration at the same base. Command `gh pr list --repo ychampion/melete --state all --limit 60 --json number,title,headRefName,baseRefName,state,mergedAt` now shows W10c PR #15 open, W12 PR #12 open, and no merged W11. Slices 4 and 5 remain dependency-gated.

## Assumptions — reviewed implementation

- Log `shared audience`: shared-space membership exposes published skills and knowledge; each job and its timeline remain private to its own principal, including within a shared space. This prevents membership from exposing an owner's private attempt context before the evaluated-sharing pipeline exists.
- Log `additive migration compatibility`: old personal-space attempts keep null principal bindings so pre-upgrade signed capabilities remain usable; new shared-space attempts require both principal and membership generation, and all newly issued attempts record their binding.

## Serialized full-suite result — 2026-09-11 22:01 UTC

- Command `bun test --max-concurrency=2` acquired `C:/Users/gamin/.melete-test.lock` at approximately 21:57 UTC and ran as W14 PID `39596`. The watchdog stopped that test tree after the 180-second budget; exit `124`. The wrapper released its own lock in `finally`.
- Log `%TEMP%/melete-w14-locked-full.stderr.log`: `376` passing test lines, `0` failing test lines and `0` error lines before the budget stop, ending during `packages/knowledge/src/retraction.test.ts`. This is a reproduced serialized-suite budget failure, not a completed green suite. The run had not yet reached the new principal integration test.
- Log `process cleanup`: the tracked Bun PID `39596`, embedded PostgreSQL parent `32876`, and transient child `6788` are absent after the watchdog. No other lane's test process or lock was interrupted.
- Command `bun test apps/melete/test/integration/principals.test.ts apps/melete/test/integration/hooks.test.ts packages/contracts/src/principals.test.ts --max-concurrency=2`: started the final focused run after the full-suite wrapper exited. It includes private shared-job timelines and submission receipt isolation, including marker-only recovery after fixture receipt rows are removed.

## Final W14 focused proof — 2026-09-11 22:03 UTC

- Command `bun test apps/melete/test/integration/principals.test.ts apps/melete/test/integration/hooks.test.ts packages/contracts/src/principals.test.ts --max-concurrency=2`: exit 0, `4 pass`, `0 fail`, `96 expect() calls`, `[79.59s]`. This executed the latest private shared-job timeline and marker-only submission receipt assertions as well as the migration, membership, stale capability, bundle selection, invalidation, deduplication and replay checks.
- Test `additive migration preserves the setup guard, login and an issued personal-space capability`: passed in `2344.00ms`. Test `member bundles select at most three shared skills; revocation fences work, replay, knowledge and old capabilities`: passed in `1547.00ms`. Test `adapter capture persists in order, deduplicates delivery and replays from the stored cursor`: passed in `1329.00ms`.
- Log `full-suite budget`: the serialized run had no assertion failure to repair before the budget stop. W14 is checking the affected existing API, job, event and broker suites separately; the incomplete full result remains explicitly red in `docs/CAPABILITIES.md` and the draft PR description.

## Existing-suite regression and first fix cycle — 2026-09-11 22:06 UTC

- Command `bun test` with the selected authentication, jobs, runner, events, replies, submissions, waits, attention, responsibility, broker and knowledge-route files: the knowledge, attention, authentication and broker checks pass. Event-router cases fail with `TypeError: undefined is not an object (evaluating c.get("owner").id)` because those standalone module fixtures do not mount the authentication middleware.
- Log `event identity fix cycle 1`: `apps/melete/src/api/events.ts` now captures `requestPrincipal()` from the same server-owned async context used by the service filters. The real app's authentication middleware establishes that identity; background stream delivery retains the captured principal. Direct internal module fixtures remain usable without an HTTP authentication fixture. The running command had already imported the old module; its event/reconnect failures will be rerun in a fresh process alongside the actual authenticated principal-isolation test.

## Receipt recovery binding and affected rerun — 2026-09-11 22:09 UTC

- Command `bun test` for the 11 selected existing suites completed with `153 pass`, `15 fail`, `1 error`, `1045 expect() calls`, `[254.47s]`. Thirteen failures share the event module's missing-context error; two receipt recovery cases returned `403` instead of their established `503 unknown_durability` response. Authentication, knowledge routes, attention, broker admission, job transitions, attempt execution, replies and waits passed.
- Test `receipt_missing yields unknown durability and never admits another job` and test `marker_only yields unknown durability and never admits another job`: internal `SubmissionService.get` reconstructed the receipt without a principal, then the authenticated retry was refused. First fix cycle: carry the recorded principal from the receipt, journal or admission marker into the uncertainty row. Recovery never adopts an arbitrary request's identity.
- Test `member bundles select at most three shared skills; revocation fences work, replay, knowledge and old capabilities`: the marker-only fixture now also performs internal recovery before confirming that another principal is still refused. Command `bun test apps/melete/test/integration/events.test.ts apps/melete/test/integration/submissions.test.ts apps/melete/test/integration/responsibility.test.ts apps/melete/test/integration/principals.test.ts --max-concurrency=2` is the fresh affected rerun after both first-cycle fixes.

## Committed implementation and final verification queue — 2026-09-11 22:28 UTC

- SHA `55fcac0382d5128dacd1e6a07d09e3b42c802636`: committed lifecycle capture, additive principal and membership authority, selected bundle context, migration, tests, generated schemas and documentation. Command `git -C C:/Users/gamin/melete-oss-w14 log -1 --format='%H%n%an <%ae>%n%cn <%ce>%n%s'` verifies author and committer `ychampion <68075205+ychampion@users.noreply.github.com>`; the working tree was clean after this commit.
- Command `bun test apps/melete/test/integration/events.test.ts apps/melete/test/integration/submissions.test.ts apps/melete/test/integration/responsibility.test.ts apps/melete/test/integration/principals.test.ts --max-concurrency=2`: all 40 named tests passed with `368 expect() calls`, including the corrected event routes and receipt recovery. The command did not exit green because fixture cleanup needed separate verification; that observation was outside the shared lock and is not a confirmed serialized timeout.
- Log `event cleanup fix cycle 2`: the event fixture now allows 15 seconds for stream shutdown and its two database pools, whose existing close operations each allow five seconds to drain. Functional assertions are unchanged. The final verification is queued under the shared lock, restricted to replay/reconnect, the two repaired receipt cases and the authenticated principal scenario.
- Command `bun run lint`: exit 0, `Checked 318 files in 856ms. No fixes applied.` Command `bun run typecheck`: exit 0 after both production fixes. Command `bunx biome check apps/melete/test/integration/events.test.ts`: exit 0 after the cleanup allowance change.
- Log `delivery boundary`: no new push has occurred while the locked verification waits; draft PR #13 still points to the earlier audit commits. The known full-suite 180-second budget failure and the real-Hermes action proof gap remain recorded independently of this focused regression check.

## Final delivery and dependency stop — 2026-09-11 22:47 UTC

- Command `bun test ./apps/melete/test/integration/events.test.ts ./apps/melete/test/integration/submissions.test.ts ./apps/melete/test/integration/responsibility.test.ts ./apps/melete/test/integration/principals.test.ts --test-name-pattern 'stream|reconnect|snapshot|receipt_missing|marker_only|principal' --max-concurrency=2`: exit 0, `17 pass`, `23 filtered out`, `0 fail`, `195 expect() calls`, `[27.45s]`. This final run proves replay, reconnect/reset, receipt recovery, migration compatibility, membership-filtered bundles, revocation and private-data isolation with clean fixture teardown. The 23 cases were deliberately outside this focused selection, not skipped for missing PostgreSQL.
- Log `focused-run authorization`: the steering explicitly allows focused runs without the shared lock. After the queued verifier never acquired it, W14 identified its own PowerShell process `17620` by the unique regression-log command and stopped only that waiting process. The other lane's Bun process `16844` and the existing lock creation time were unchanged. The passing command above used the authorized focused path; no serialized cleanup timeout is claimed.
- SHA `55fcac0382d5128dacd1e6a07d09e3b42c802636`: the implementation is committed. SHA `42fc8fa0706cb8df8997449eecf0cbfd4aaac76d`: the verification progress was committed and pushed with `git -C C:/Users/gamin/melete-oss-w14 push -u origin lane/w14-capabilities`, exit 0. Command `gh pr edit 13 --repo ychampion/melete --title 'Capture lifecycle hooks and fence shared context by principal' --body-file <temp-body>` updated the existing draft.
- Command `gh pr view 13 --repo ychampion/melete --json number,url,title,baseRefName,headRefName,headRefOid,isDraft,state`: verified https://github.com/ychampion/melete/pull/13, `OPEN`, `isDraft=true`, base `integration`, head `lane/w14-capabilities`, and pushed head `42fc8fa0706cb8df8997449eecf0cbfd4aaac76d` before this final report append. No merge or force push occurred.
- Command `git -C C:/Users/gamin/melete-oss-w14 fetch origin integration`: final exit 0; SHA `9484023cabd32b786cb4d336dec818f441cd0cc1` remains the integration head. Command `gh pr list --repo ychampion/melete --state all --limit 60 --json number,title,headRefName,baseRefName,state,mergedAt`: W10c PR #15 and W12 PR #12 remain open, with no merged W11. Slices 4 and 5 stop at the stated dependency gate.
- Log `slice 2`: lifecycle observations, observer failure isolation, adapter ordering, persistence, deduplication and replay are implemented and tested at their recorded boundaries. The last permitted real-Hermes test still failed its broker-action count (`0`, expected `1`), with `Tool files.read handler returned unsupported result type: dict`; two fix cycles are exhausted. Real compaction and a passing real-server effect proof remain unverified.
- Log `slice 3`: additional principals, private personal spaces, shared memberships, audience checks, at-most-three skill selection in actual bundles, generation fencing, delivered-context invalidation and refusal after revocation are implemented and tested. The final receipt test also preserves its principal through internal recovery and refuses another principal. Invitation UI and evaluated sharing remain outside this delivered primitive.
- Command `bun test --max-concurrency=2` under the shared lock: the full suite remains incomplete after the confirmed 180-second budget stop, exit `124`, with 376 passing test lines and no assertion failures before termination. The affected regressions found in the later focused run were fixed and their final checks passed. No all-checks-green or DONE claim is made.
- Log `final proof status`: `docs/CAPABILITIES.md` carries the three-state matrix, exact test names and proof limits. Lint, type checking, generated OpenAPI/client checks, Python observer checks and 12 Compose configuration assertions have passed as recorded above. Docker isolation and the single MCP/learning capability-chain proof remain unverified; no real-provider credentials were used.

PROOF PENDING: waiting for W10c/W11 to merge

## Resumed capability proof - 2026-09-12 04:22 UTC
- `git fetch origin` and `git merge --no-edit origin/integration` fast-forwarded W14 from `5135678` to `55b6a50`; integration contains the landed dependencies and preserved W14 tests with migration `0027`.
- `bun install` completed; `.hermes-venv/Scripts/python.exe` and `.hermes-src` are the retained local engine installation from the prior proof.
## Assumptions
- `REPORT.md` was removed by integration; restoring its W14 history from `5135678` preserves the brief's append-only reporting requirement.
- `capability-proof.test.ts` will exercise the landed service seams with the real local Hermes HTTP server and only a scripted model provider. Missing capabilities will be reported without substitute success claims.
- `bun test` will run only once at the end under `C:/Users/gamin/.melete-test.lock`; focused checks run sequentially. This resumed campaign starts at 04:20 UTC with the original five-hour cap and two-fix-cycle limit per check.

## Live capability checks - 2026-09-12 04:44 UTC
- `MELETE_CAPABILITY_AUDIT=1 bun test ./apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1` starts the real pinned Hermes HTTP engine through `bootstrap()` with a fake model provider, Postgres and pg-boss.
- `capability-evidence-c0d9bP`: the initial run reproduced zero broker actions after `load_tool` and `proposal_rejected` because the supervised adapter lacked catalog continuation and the proposal gateway lacked the configured fake callback.
- `capability-evidence-NDZqHh`: the strengthened identity assertion reproduced a member receiving `role: owner`, `audience: private` from `startServiceMemory.scopeForJob`; memory scopes now retain the current principal and membership generation.
- `capability-evidence-ioC7SN`: held-out evaluation rejected `no_family_improvement`; captured HTTP prompts proved catalog enrichment had overwritten the evaluated procedure. Catalog enrichment now narrows the checked bundle selection, preserving evaluated procedures and audience filtering.
- `capability-proof.test.ts`: the first continuation exposed `hook attempt identity mismatch`. Observer capture now keeps the original attempt identity and a distinct continuation capture prefix; the process supervisor prepares the same hash-checked bridge as the image.
- `capability-evidence-qYfk8c`: the correction, validation/sealed evaluation, owner canary reuse, activation, rollback, member private-procedure refusal, shared skill selection and membership revocation stages passed through real Hermes. The MCP read was recorded `unknown`; its job became `needs_reconciliation`. This is a recorded outcome, not a confirmed shared-lock suite timeout.
- `GET /jobs/job_01M29YEXTZMGF7YGYRTTEDQ4CR/events`: durable events 15 and 23 show successful `search_tools` and `load_tool`; action `act_01M29YF71GKSRRE1G5EH4C4RV2` has no receipt and event 49 records `outcome: unknown`. Two discovery integration fix cycles are exhausted; no successful MCP effect is claimed.
- `POST /connections` returns 404; `mcpServerConfig` is operator-file-only and `openConfiguredMcpConnector` refuses stdio service launch. `procedureScope` accepts only owner/private. Mid-session MCP installation, MCP reconnect/auth refresh and evaluated shared promotion remain missing, despite the dependencies having landed.

## Final strict capability result - 2026-09-12 04:53 UTC
- `MELETE_CAPABILITY_PROOF=1 bun test ./apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1`: exit 1, `0 pass`, `1 fail`, `37 expect() calls`, `180.60s`; the named test is `real Hermes capability chain: discovery, hooks, learning, teammate context and revocation`.
- `capability-evidence-1xOgEE`: final evidence is `%TEMP%/melete-w14-capability-1xOgEE/capability-evidence.json`, SHA-256 `a6b943ddc80986b94661637e43a89092e28965baa17c51c8baaef63dcc96b40d`; provider requests are retained alongside it. All records are fixture data; the fixture's work and space directories were removed on teardown.
- `dynamic discovery and broker receipt`: the real engine searches, loads and continues with the tool schema, but the action is `unknown`, its receipt is null, and the local MCP server receives zero `tools/call` requests. The job enters `needs_reconciliation`. No successful effect or reconnect is claimed; the two permitted discovery integration fix cycles are exhausted.
- `correction, evaluation, private reuse and rollback`: passed through real Hermes HTTP. Validation and sealed evaluation both return `held_out_improvement_without_regression`; each phase has three paired record tasks with baseline 1/3 and candidate 3/3, plus scope and memory checks. Owner canary reuse on a different task, activation, private-procedure refusal for a member, and rollback all pass.
- `teammate audience isolation and revocation`: passed through real Hermes HTTP. A matching private file does not displace authorized `alpha`, `beta`, `gamma`; neither the bundle nor provider requests contains the private canary. Revocation cancels queued work, refuses reads and admission, denies the old capability and excludes shared skills from personal context.
- `capability-proof.test.ts` records 18 durable hook events, including session start/end, pre/post tool and distinct continuation capture IDs. The MCP assertion fails before the later hook assertions; the trace is observed evidence, while `hooks.test.ts` and `test_hooks.py` supply passing persistence, replay, failure-isolation and continuation checks. Real compaction remains unverified.

| Capability checked by `capability-proof.test.ts` | Status | Test stage or code reason |
|---|---|---|
| Dynamic tool discovery | implemented-but-unverified | `dynamic discovery and broker receipt`: search/load/continuation observed; MCP receipt assertion fails with `unknown`. |
| MCP connect after session start | missing | `mid-session installation and disconnect boundary`: running-job `POST /connections` is 404; operator-file startup only, stdio service launch disabled. |
| MCP disconnect recovery | missing | `mid-session installation and disconnect boundary`: health failure/recovery is observed, but no MCP reconnect repair callback or exactly-once job recovery exists. |
| Connection auth refresh and reconnect | missing | `openConfiguredMcpConnector` has no credential-refresh/reconnect callback. |
| Lifecycle hooks | implemented-but-unverified | `hooks.test.ts` and `test_hooks.py` pass; real trace has 18 hook events, while real compaction is unverified. |
| Automatic skill selection | implemented-and-tested | `teammate audience isolation and revocation` passes with three authorized skills and no private canary. |
| Correction, candidate, evaluation, promotion, rollback | implemented-and-tested | `correction, evaluation, private reuse and rollback` passes with owner/private promotion. |
| Teammate reuse of an evaluated shared skill | missing | `procedureScope` permits owner/private only; `learning-three-act.test.ts` and the real member task refuse private procedure delivery. |
| Revocation prevents subsequent use | implemented-and-tested | `teammate audience isolation and revocation` passes; `principals.test.ts` also covers delivered-context invalidation, gateway fencing and regrant. |

## Checks before the single full suite - 2026-09-12 04:53 UTC
- `bun test ./apps/melete/test/integration/learning-three-act.test.ts ./apps/melete/test/integration/capability-catalog.test.ts ./apps/melete/test/integration/principals.test.ts ./apps/melete/test/integration/hooks.test.ts ./packages/runtime-hermes/src ./apps/melete/src/runtime/hermes.test.ts ./apps/melete/src/runtime/supervisor.test.ts --max-concurrency=1 --timeout=30000`: exit 0, `77 pass`, `1 skip`, `0 fail`, `329 expect() calls`, `25.24s`; the skip is a platform-specific supervisor check. The later metadata-availability adjustment is additionally exercised by the final real teammate stage.
- `bun run test:plugin`: exit 0, `61 passed in 25.77s`, including the new continuation identity/error-frame regression in `test_hooks.py`.
- `bun run typecheck`: final exit 0. `bun run lint`: final exit 0, `Checked 532 files in 605ms`, one existing unused empty import warning in `apps/melete/test/integration/postgres.ts`. `git diff --check`: exit 0.
- `bun run compose:check`: exit 0, `19 checks`; these validate declarations and do not represent a Docker execution on this Windows host.
- `gh pr view 13 --repo ychampion/melete --json number,url,state,isDraft,mergeCommit,baseRefName,headRefName`: PR #13 is already `MERGED` at `5720787dc2d55361df5b5d2c91553e12763fb636`, base `integration`. The conditional `gh pr ready` does not apply to a failed proof or an already merged PR; the follow-up commits will be pushed to the lane.

## Single locked full suite and focused repair - 2026-09-12 05:02 UTC
- SHA `5eb904a2a3ed6d93e4ea680180bd2af471b4ef72`: committed the service integration fixes, real capability test, provider fixture and current capability matrix. `git log -1` verifies author and committer `ychampion <68075205+ychampion@users.noreply.github.com>`.
- `bun run openapi` followed by `bun run client:generate` started from a clean tree at `5eb904a`; both exited 0, and `git diff --exit-code` plus `git status --porcelain` verified no generated or untracked changes.
- `bun test --max-concurrency=1 --timeout=30000`: ran exactly once for the full suite, under `C:/Users/gamin/.melete-test.lock` with `w14-owner.json`. The 180-second watchdog recorded exit `124` at `180.70s`, with `990` passing lines, `1` failing line and `26` skipped lines before termination. The PowerShell wrapper returned failure; the full suite did not finish and is not green.
- `melete-w14-resumed-full-20260912.result.json`: retained under `%TEMP%` alongside `.stdout.log` and `.stderr.log`; `%TEMP%/melete-w14-final-suite.ps1` records the single invocation, owned child PID, budget and ownership-checked lock release. Only the owned Bun PID `14404` and its descendants were terminated; subsequent process inspection found them absent and the shared lock removed.
- `first-job provisioning reads actual Markdown then excludes retraction on the next attempt`: the full-suite failure was a `30015ms` timeout at the database rejection check. Its fixture uses the unchanged deployment-memory bootstrap, not the service-memory path modified in this follow-up.
- `context.test.ts` fix cycle 1: explicitly awaiting the refused database operation made that first test pass in `2640ms`; the same focused run exposed a timeout in the last test's remaining async rejection matcher. After its database closed, its own Bun process `41116` was identified by the exact command and stopped to end pg-boss reconnect retries. This later focused timeout was not a second shared-lock run.
- `context.test.ts` fix cycle 2: all three database rejection checks now await the operation before checking `MemoryError` and its exact code (`scope_denied`, `context_invalidated`, `stale_attempt`). No product behavior or authority assertion was weakened.
- `bun test ./apps/melete/src/runtime/context.test.ts --max-concurrency=1 --timeout=30000`: final exit 0, `5 pass`, `0 fail`, `24 expect() calls`, `21.62s`, including clean fixture teardown. This focused repair followed the newly discovered full-suite failure; the full suite was not repeated, as instructed.
- `docs/CAPABILITIES.md` and `.agents/notes/0021-hermes-capability-audit.md`: current nine-capability tables replace the old dependency wait. The remaining proof gaps are MCP receipt delivery, mid-session installation, MCP disconnect/auth recovery, evaluated shared promotion and real compaction. The original DONE condition remains unmet.
- `bun run typecheck`: final exit 0 after the context-test repair. `bun run lint`: final exit 0, `Checked 532 files in 592ms`, with the same pre-existing empty-import warning. `bunx biome check apps/melete/src/runtime/context.test.ts`: exit 0.

## Final delivery status - 2026-09-12 05:02 UTC
- `lane/w14-capabilities`: the implementation at `5eb904a` and the context-test/report follow-up are the delivery commits; `git push -u origin lane/w14-capabilities` is the authorized target. No force push, merge or change to another worktree is part of this continuation.
- `capability-proof.test.ts`: the strict proof remains red, with the implemented owner-private learning and revocation stages passing. Missing MCP installation/recovery and evaluated sharing remain explicit implementation gaps; the dependency wait from the earlier report is no longer current.
- `bun test --max-concurrency=1 --timeout=30000`: the one locked full-suite result remains incomplete after its 180-second budget, even though the subsequently repaired context checks pass. The report does not claim all checks pass, a successful complete capability chain, or DONE.

## Release-gate continuation and MCP receipt reproduction - 2026-09-12 05:23 UTC
- SHA `c83c3e5ae26248d32be93185cd319d8d755e0985`: clean starting point for the new sequential four-item campaign, started `2026-09-12 05:07 UTC`; the five-hour stop is `10:07 UTC`. The new full suite will run once at the end under the shared lock.
- `MELETE_CAPABILITY_PROOF=1 MELETE_CAPABILITY_STAGE=dynamic bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1`: fresh reproduction fails in `35.65s`, `8 expect() calls`, with MCP action `unknown`, receipt null, and zero worker calls; evidence `melete-w14-capability-RqbJeA`.
- `capability-evidence-0gCi3X` and `capability-evidence-51XTB8`: temporary transport tracing shows successful initialize/notification/list, then an MCP `tools/call` fetch abort at `10014ms`; the fixture never receives that HTTP request. Direct `POST /actions` through the effect listener and a separate idle-connection probe both pass, narrowing the failure to pooled HTTP use during the real runtime stream.
- `capability-evidence-zXHVZX`: disabling pooling in the diagnostic wrapper makes the real MCP stage pass: `1 pass`, `16 expect() calls`, `22.18s`, one worker call, receipt and lifecycle events. The diagnostic wrapper and idle-only probe were removed; `openHttpMcpTransport` now sets `keepalive: false` on actual service exchanges. The MCP session header, scopes, effect classes, intent keys and unknown-acknowledgement rules are unchanged.
- `openHttpMcpTransport`: [Bun's fetch documentation](https://bun.sh/docs/runtime/networking/fetch) defines `keepalive: false` as disabling connection reuse. This is a narrow workaround for the reproduced pinned Windows Bun transport behavior; no request is replayed to conceal the failure.
- `MELETE_CAPABILITY_PROOF=1 bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1`: the whole proof after item 1 returns exit 1 only on its three remaining missing-capability entries, `45 expect() calls`, `208.50s`; evidence `melete-w14-capability-VUkw9l`. All implemented stages pass, including one MCP call/receipt, hooks, private learning and revocation.
- `bun test apps/melete/src/connectors/mcp.test.ts apps/melete/test/integration/mcp.test.ts --max-concurrency=1 --timeout=30000`: exit 0, `20 pass`, `0 fail`, `120 expect() calls`, `13.43s`; covers real stdio and HTTP, acknowledgement loss, unchanged approvals/origin warnings, repeated intent and revocation. `bun run typecheck`, focused Biome checks and `git diff --check` also pass for item 1.

## Runtime MCP installation - 2026-09-12 05:41 UTC
- SHA `27e2f5b9f68f5e09deed336900299ce6b87de86d`: item 1 is committed as `Deliver MCP receipts while the runtime stream is open`.
- `bun test apps/melete/test/integration/runtime-mcp.test.ts --max-concurrency=1 --timeout=30000`: reproduction before implementation returns 404 instead of 201, exit 1 in `9.64s`; the implemented route subsequently passes `35 expect() calls` in `12.23s`.
- `runtime-mcp.test.ts`: an already claimed attempt initially lacks the MCP scope, observes no tools during `connecting`, then searches, loads and receives one action receipt after installation. The test also proves fixed-capability denial, unauthenticated and other-principal denial, public-compartment denial, removed-scope denial, duplicate rejection, reopening from database configuration and persisted error state without a worker.
- `createConnectionRequest` and migration `0033_busy_whiplash.sql`: additive MCP HTTP policy/configuration and optional response setup state; the database persists only endpoint/policy here. `bun run openapi` and `bun run client:generate` regenerate the public boundary. The unchanged stdio service launcher remains disabled.
- `AttemptRunner` assumption: service-issued attempts that are principal-bound and have no explicit fixed scope restriction opt into signed `live_connection_scopes`; this delegates only current operator connection grants within the existing attempt/principal/member/space/compartment/agent fences. Restricted and legacy capabilities retain their fixed scopes. This permits an owner installation to reach a running session without restarting its engine or silently changing a restricted token.
- `MELETE_CAPABILITY_PROOF=1 bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1 --timeout=300000`: whole real-Hermes proof after item 2 executes `48 expect() calls` in `269.11s`; every implemented stage passes. The strict final assertion remains red only for MCP recovery/refresh and evaluated teammate reuse. Evidence: `%TEMP%/melete-w14-capability-svpYtI/capability-evidence.json`.
- `capability-proof.test.ts`: installation now happens through the authenticated owner route inside a running attempt, with no preinstalled MCP row or operator file. Its initial capability has no `mcp_fixture.read` scope; the same attempt performs search/load/continuation and exactly one MCP call with a receipt and 23 lifecycle events.
- `bun run typecheck`: exit 0 after adding explicit response parsing in the test. `bunx biome check --write` corrected the route formatting and normalized the entry-point import; final focused checks follow below.
- `bun test apps/melete/test/integration/runtime-mcp.test.ts apps/melete/test/integration/mcp.test.ts apps/melete/src/connectors/mcp.test.ts apps/melete/src/broker/catalog.test.ts --max-concurrency=1 --timeout=30000`: final item-2 focused suite exits 0, `25 pass`, `0 fail`, `167 expect() calls`, `22.17s`. Focused Biome checks and `git diff --check` exit 0.

## MCP recovery and credential boundary - 2026-09-12 06:06 UTC
- SHA `3eda4fe450821ae70a9b22a90fc4a673f0750e6d`: item 2 is committed as `Install operator MCP connections during running sessions`.
- `bun test apps/melete/test/integration/mcp-repair.test.ts --max-concurrency=1 --timeout=30000`: the item-3 reproduction was written before product changes and failed with `unknown` instead of `succeeded`, `10.76s`; a session-terminated HTTP request had not executed, but the connector had no reconnect path.
- `mcp-repair.test.ts`: HTTP and stdio fixtures now exercise reconnect, expiry, revocation, and revocation during refresh through the real broker and sealed store. Persistent HTTP termination stops after three attempts; a repeated 401 refreshes once; a truncated acknowledgement stays unknown with one observed effect and no reconnect or refresh.
- `mcp-transport.ts` and `mcp.ts`: typed failures are limited to proved pre-dispatch loss, protocol session termination and HTTP authentication refusal. [MCP transport rules](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports) require a new initialization after session termination and do not make a dropped acknowledgement safe to replay. A reconnect must reproduce the pinned tool catalog; operator effects, approvals, scopes and intent bytes are unchanged.
- `mcp-credentials.ts`: authenticated installation seals the operator credential record. Transport access and one refresh use the sealed store, an operator-pinned TLS token endpoint (loopback fixtures permitted), bounded responses, no redirects and a compare-and-swap on current connection status, generation, scopes and secret reference. HTTP and stdio tests prove a revoked grant cannot publish the rotated token or dispatch the action. Service stdio remains disabled without an isolated launcher.
- `MELETE_CAPABILITY_PROOF=1 bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1 --timeout=600000`: the first item-3 run (`melete-w14-capability-JJ5RC5`, `237.33s`) exposed a scripted-provider continuation mistake; the fixture ended before the loaded tool appeared. After correcting that fixture, `melete-w14-capability-YNEiie` passes discovery, reconnect and refresh but fails the open-question assertion after revocation (`70 expect() calls`, `284.62s`). Private learning and membership revocation remain green.
- `bun test apps/melete/test/integration/mcp-repair.test.ts --test-name-pattern 'revoked obeys' --max-concurrency=1 --timeout=30000`: a focused reproduction confirms that both transports stop the action, but `AttemptRunner.commitOutcome(completed)` completes the job and withdraws its question. The first fixture needed a live lease; the corrected reproduction fails `completed` versus `waiting_for_input`, `12 expect() calls`, `36.72s`, before the runner change.
- `AttemptRunner.finish`: the second item-3 product fix preserves a broker-created open revocation question against a runtime completion claim, including the last budgeted attempt. Owner input remains the resolution path; the runner commits `waiting_for_input` and keeps the question open.
- `bun test apps/melete/test/integration/mcp-repair.test.ts apps/melete/test/integration/runtime-mcp.test.ts apps/melete/test/integration/mcp.test.ts apps/melete/src/connectors/mcp.test.ts apps/melete/src/broker/repair.test.ts --max-concurrency=1 --timeout=30000`: final focused run exits 0, `70 pass`, `0 fail`, `394 expect() calls`, `16.59s`. The whole real-Hermes proof is running again after the runner guard; no full suite has run in this campaign yet.
- `MELETE_CAPABILITY_PROOF=1 bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1 --timeout=600000`: final item-3 whole proof executes `75 expect() calls` in `206.98s`; all implemented stages pass. It exits 1 only for the remaining evaluated-teammate-reuse gap. Evidence `%TEMP%/melete-w14-capability-CsJFVK/capability-evidence.json` records three total MCP calls, three initialized sessions, one refresh, and an open revocation question with a committed `waiting_for_input` outcome.
- `bun test apps/melete/test/integration/mcp-repair.test.ts apps/melete/src/broker/repair.test.ts --max-concurrency=1 --timeout=30000`: `50 pass`, `0 fail`, `240 expect() calls`, `11.36s`, including revocation during reconnect and question preservation on the final budgeted attempt. `bun test apps/melete/test/integration/runner.test.ts apps/melete/test/integration/repair.test.ts --max-concurrency=1 --timeout=30000`: `50 pass`, `0 fail`, `366 expect() calls`, `31.90s`. `bun run typecheck`, focused Biome and `git diff --check` pass for item 3.

## Evaluated procedure sharing - 2026-09-12 06:33 UTC
- SHA `92806b6468d2b7ce3ae3834587623349c1953de4`: item 3 is committed as `Reconnect MCP sessions and refresh sealed credentials through repair`.
- `bun test apps/melete/test/integration/shared-procedure.test.ts --max-concurrency=1 --timeout=30000`: the item-4 reproduction created a real correction, proposal, evaluation and private canary, then failed the new `POST /procedures/:id/activate` request with 400 instead of 200 (`22 expect() calls`, `14.94s`) before product changes.
- `procedureActivationRequest` and migration `0034_clumsy_harry_osborn.sql`: activation adds `scope: private | space`, default private; the promotion record stores scope and the authenticated principal. Applicability and the evaluated body hash stay separate from this explicit delivery grant. Canary delivery remains private, and space activation requires the origin shared-space owner and the existing evaluation/canary gates.
- `selectProcedureSkills`: current membership is checked under the existing space authority lock before compiled-body delivery. The member receives no source episode, private correction or evaluation context. Episode/proposal/procedure access also checks the source job principal. Sealed evaluation arms retain their trusted principal in isolated spaces.
- `shared-procedure.test.ts`: final focused result is `1 pass`, `0 fail`, `57 expect() calls`, `18.88s`. It covers private default, member refusal before sharing, A-to-B reuse on different data, private source refusal, forged-principal refusal, other-space/public exclusion, revoked queued/live work without dispatch, and rollback.
- `capability-proof.test.ts`: the first whole item-4 run passed dynamic MCP, reconnect, refresh and evaluated teammate reuse, then rejected a new queued fixture whose display title was not a valid template ID (`melete-w14-capability-ldhehY`, `75 expect() calls`, `202.51s`). The fixture now uses `w14-revoke-queued`; the whole proof is running again without another product change.
- `bun run openapi`, `bun run client:generate`, `bun run typecheck`, focused Biome and `git diff --check`: all passed after item-4 implementation. The final regeneration comparison, plugin checks and single locked full suite remain pending.
- `MELETE_CAPABILITY_PROOF=1 bun test apps/melete/test/integration/capability-proof.test.ts --max-concurrency=1`: final whole item-4 proof exits 0, `1 pass`, `0 fail`, `85 expect() calls`, `208.62s`. All five stages pass; `missing` and `failures` are both empty.
- `capability-evidence-JAbcAJ`: `%TEMP%/melete-w14-capability-JAbcAJ/capability-evidence.json`, SHA-256 `899c68b5b6470201ea378e0c2e70306255c499e160a9a317634dfb1aa3e7fa31`; the real discovery stage records 23 hooks and one receipt, repair totals three MCP calls/three sessions/one refresh, and the final stage proves revoked queued reuse delivers no bundle or action before owner rollback.
- `docs/CAPABILITIES.md` and `.agents/notes/0021-hermes-capability-audit.md`: the release-gate matrix now has eight implemented-and-tested rows, one implemented-but-unverified lifecycle row because actual compaction has not been exercised, and zero missing rows. Service stdio launch stays disabled pending OS isolation; its repair callbacks are covered by explicit fixtures.
- `bun test apps/melete/test/integration/learning-episodes.test.ts apps/melete/test/integration/learning-proposals.test.ts apps/melete/test/integration/learning-evaluation.test.ts apps/melete/test/integration/learning-three-act.test.ts apps/melete/test/integration/principals.test.ts apps/melete/test/integration/shared-procedure.test.ts --max-concurrency=1 --timeout=30000`: final item-4 regressions exit 0, `26 pass`, `0 fail`, `308 expect() calls`, `46.26s`.
- `bun run typecheck`: final exit 0. `bun run lint`: exit 0, `Checked 540 files in 889ms`, with one pre-existing empty-import warning in `apps/melete/test/integration/postgres.ts`. `bun run test:plugin`: exit 0, `61 passed in 25.45s`. `bun run compose:check`: exit 0, 19 declaration checks; no Docker execution is claimed.

## Final release-gate checks - 2026-09-12 06:41 UTC
- SHA `e101876`: item 4 is committed as `Share evaluated procedures with authorized space members`; `git log -4` verifies the required author and committer identity on all four ordered item commits (`27e2f5b`, `3eda4fe`, `92806b6`, `e101876`).
- `bun run db:generate`, `bun run openapi`, `bun run client:generate`: exit 0 from a clean tree at `e101876`; Drizzle reports no schema changes, `git diff --exit-code` passes and `git status --porcelain` remains empty afterward.
- `bun test --max-concurrency=1 --timeout=30000`: the full suite ran exactly once under `C:/Users/gamin/.melete-test.lock` with an ownership marker. The 180-second watchdog recorded exit `124`, `180.75s`, `1312` passing lines, `0` failing lines and `29` skipped lines. This is an incomplete suite, not a green complete run; it was not repeated.
- `melete-w14-release-gate-full-20260912.result.json`: result and adjacent stdout/stderr logs are retained under `%TEMP%`; `%TEMP%/melete-w14-release-gate-suite.ps1` records the exact command and watchdog. The last output entered `memory.test.ts` after passing learning and MCP integration checks; no individual test failure occurred before the whole-suite deadline.
- `Get-Process -Id 26792,17284,8780,42008,38404,20076,36448,34600`: none of the owned test processes remain after termination. `Test-Path C:/Users/gamin/.melete-test.lock` returns false after ownership-checked release; no other worktree's lock or process was removed.
- `capability-proof.test.ts`: the strict real pinned Hermes HTTP proof is green (`85 expect() calls`, `208.62s`, evidence `melete-w14-capability-JAbcAJ`) and is separate from the budget-limited ordinary suite. All four requested implementation gaps are closed with reproduction-first changes and a whole-proof run after each item.

| Capability from `docs/CAPABILITIES.md` | Final status | Test evidence or remaining reason |
|---|---|---|
| Dynamic tool discovery | implemented-and-tested | `dynamic discovery and broker receipt`: search/load/continuation, one actual MCP call and receipt. |
| MCP connect after session start | implemented-and-tested | `mid-session installation, disconnect recovery, refresh and revocation`: authenticated installation in a running attempt, live search/load/use. |
| MCP disconnect recovery | implemented-and-tested | `mcp-repair.test.ts` and the real repair stage: bounded reconnect, same intent, one receipt; lost acknowledgement stays unknown without replay. |
| Connection auth refresh and reconnect | implemented-and-tested | `mcp-repair.test.ts` and the real repair stage: one sealed refresh; revoked grant stops dispatch and preserves an open question. |
| Lifecycle hooks | implemented-but-unverified | `hooks.test.ts`, `test_hooks.py` and 23 real lifecycle events pass; actual committed compaction has not been exercised. |
| Automatic skill selection | implemented-and-tested | `teammate audience isolation and revocation`: exactly alpha/beta/gamma and no private canary in actual provider requests. |
| Correction, candidate, evaluation, promotion, rollback | implemented-and-tested | `correction, evaluation, private canary and evaluated teammate reuse` plus the final rollback stage. |
| Teammate reuse of an evaluated shared skill | implemented-and-tested | `shared-procedure.test.ts` and real A-to-B different-task reuse after explicit space activation. |
| Revocation prevents subsequent use | implemented-and-tested | `evaluated shared procedure revocation and rollback`: queued reuse is cancelled, selection refused, no new bundle or action. |

## Final delivery scope - 2026-09-12 06:41 UTC
- `docs/CAPABILITIES.md`: eight implemented-and-tested rows, one implemented-but-unverified row, zero missing rows. Actual compaction and Docker execution remain unverified; production stdio launch stays disabled because this host supplies no isolated service launcher. Both stdio and HTTP repair fixtures pass.
- `REPORT.md`: the original all-checks-pass DONE condition is not claimed because the single ordinary full suite did not finish inside its mandated budget. No failing assertion remains in the executed focused or real-Hermes checks.
- `git push -u origin lane/w14-capabilities`: the authorized delivery target is this lane only. `gh pr view 13 --repo ychampion/melete` confirms PR #13 was already merged at `5720787dc2d55361df5b5d2c91553e12763fb636`; with no missing rows, the requested `gh pr ready 13 --repo ychampion/melete` will be attempted after pushing and its exact result recorded.

## Push and PR command result - 2026-09-12 06:42 UTC
- SHA `733b074`: the final proof/suite report is committed. `git push -u origin lane/w14-capabilities` succeeds, advancing the remote from `c83c3e5` to `733b074` with no force push.
- `gh pr ready 13 --repo ychampion/melete`: exit 1, `Pull request ychampion/melete#13 is closed. Only draft pull requests can be marked as "ready for review"`. PR #13 was already merged; the requested command cannot reopen or mark it ready. No replacement PR or merge was created.
- `REPORT.md`: delivery closes the four requested implementation gaps and passes the strict real-Hermes proof. The remaining limits are unverified real compaction, disabled unisolated production stdio, and an incomplete single ordinary suite after its 180-second budget. This final command-result note is committed and pushed to the same lane.
